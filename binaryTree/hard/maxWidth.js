// Find the max width of the binary tree

class newNode {
  constructor(val, level) {
    this.val = val;
    this.level = level;
  }
}

function maxWidth(node) {
  let queue = [];
  let modNode = new newNode(node.val, 0);

  while (queue.length) {}
}
